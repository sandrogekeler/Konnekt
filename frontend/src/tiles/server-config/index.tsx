import type { TileProps } from '../../types'

export function ServerConfigTile(_props: TileProps) {
  return (
    <div className="flex items-center justify-center h-full text-white/30 text-sm font-mono">
      Server Config — placeholder
    </div>
  )
}
